{
  "name": "@acd/admin",
  "version": "1.0.0",
  "description": "Admin panel, multi-tenancy, affiliates, DX tooling, scaling, product auth, publishing, and cross-product data for ACD",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.0"
  },
  "license": "UNLICENSED",
  "private": true
}
